# from setuptools import setup, find_packages
#
# setup(name='yahoo_finance_api',
#       version='1.0',
#       packages=find_packages(),
#       scripts=['manage.py'])

if __name__ == "__main__":
    import setuptools
    setuptools.setup()